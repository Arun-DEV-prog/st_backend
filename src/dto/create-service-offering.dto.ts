import { IsOptional } from 'class-validator';

// ServiceOffering entity has no additional fields besides id and timestamps
export class CreateServiceOfferingDto {
  // No fields required as per entity definition
  // Add fields here if needed in the future
}